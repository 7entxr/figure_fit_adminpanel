import 'package:flutter/material.dart';

class RatingsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('RatingsScreen')),
      body: Center(child: Text('RatingsScreen Page', style: TextStyle(fontSize: 24))),
    );
  }
}
