import 'package:flutter/material.dart';

class GiftBoxScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('GiftBoxScreen')),
      body: Center(child: Text('GiftBoxScreen Page', style: TextStyle(fontSize: 24))),
    );
  }
}
