import 'package:flutter/material.dart';

class WishlistProductScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('WishlistProductScreen')),
      body: Center(child: Text('WishlistProductScreen Page', style: TextStyle(fontSize: 24))),
    );
  }
}
